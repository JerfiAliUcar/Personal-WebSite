@model List<Education>

<h2 class="mt-3">Eğitim Bilgileri</h2>
<div class="mt-4" style="padding-left:unset">

    <span class="text-danger">@ViewBag.message</span>
    <table class="table table-dark  table-striped-columns">
        <thead>
            <tr>
                <th>ID</th>
                <th>Başlık</th>
                <th>Alt Başlık</th>
                <th>Alt Başlık 2</th>
                <th>GNO</th>
                <th>Tarih</th>
            </tr>
        </thead>
        @foreach (Education item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Title</td>
                <td>@item.SubTitle1</td>
                <td>@item.SubTitle2</td>
                <td>@item.GNO</td>
                <td>@item.Tarih</td>

                <td>
                    <a asp-area="Admin" asp-controller="Education" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning">Düzenle</a>

                    <a asp-area="Admin" asp-controller="Education" asp-action="Delete" asp-route-id="@item.Id" class="btn-danger btn">Sil</a>
                </td>
            </tr>

        }
    </table>
    <div>
        <a asp-area="Admin" asp-controller="Education" asp-action="Create" class="btn btn-primary"> Create New </a>
    </div>

</div>